# evopilot-harness 4.5.0

> Status: candidate. Target revision 15 (`sha256:51aef4c43bc4fb808fd115c5735cea7556d52b693affcb9eab6e60197a678abc`) passed all required acceptance and was separately authorized for commit, push, tag `v4.5.0`, GitHub Release, and npm publication. Public release status is recorded only after the tag workflows complete.

## Candidate scope

- Add a user-owned, non-executable 业务分类方案 contract with independent 业务领域 and 产品或系统类型 axes.
- Validate and canonicalize the scheme before Source access; the Engine-owned Semantic Foundation contains no built-in business categories.
- Build an immutable taxonomy-blind and LLM-free Source concept hypothesis from static evidence only.
- Normalize local files, local directories, local Git repositories, GitHub repositories, controlled fixtures and exact ordered attachment sets through `SourceDescriptor/v1`; use bounded read-only Git acquisition, full commit pinning and immutable snapshots without executing Source content.
- Record deterministic exact, BM25, embedding, and structured retrieval signals; call the configured Advisor exactly once for each new attempt and keep it non-authoritative.
- Return one deterministic classification result: covered, missing category, insufficient Source evidence, or ambiguity. Positive matches and extension suggestions require two immutable citations from two independent non-LLM evidence families.
- Require a separate explicit human choice after a complete match before entering Harness Eligibility.
- Retain the complete v4.4 product lifecycle after handoff: professional Source-to-Harness reasoning, Catalog comparison, all Harness outcomes, Proposal Review, explicit approval, separate publication authorization, Catalog validation, recovery, and safe close.
- Establish a fresh v4.5 representation baseline. Pre-v4.5 data and protocol representations are not read or migrated; this does not remove product capability.

## Acceptance closure

Target revision 15 closed all 203 Functional and Capability criteria, five composite non-WorkBuddy machine variants, five complete designated-human WorkBuddy journeys, the frozen 48-case classification matrix, independent and hostile Host checks, the retained v4.4 capability baseline, and cumulative `NO_REGRESSION`. The live GitHub acceptance uses one independently selected, candidate-blind, pinned Source that must pass one-of-one; broader outcome coverage remains in the fixed Source Portfolio and deterministic matrices. The three packaged GitHub discovery schemas were corrected to accept the compact one-Source contract, rebound to release package `sha256:853aa47acfee7838c90bb6890edbf722772bc6ed891b73bfdbd5292737e2b91b`, and fully revalidated without creating another Candidate. See [v4.5.0 Acceptance Closure](../operations/v4.5.0-acceptance.md).
