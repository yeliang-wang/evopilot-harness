# evopilot-harness 4.4.0

> Status: released. Target revision 10 closed AC01–AC85, inherited AC01–AC26, RC01–RC15, and `NO_REGRESSION`; commit, push, tag `v4.4.0`, GitHub Release, and npm publication were separately authorized and completed.

## Release scope

- Add Engine-owned `BusinessDecisionView`, `ComplianceAuditEnvelope`, `SourceToHarnessReasoningMap`, and finite `DecisionDefinition` contracts.
- Make the exact business view the primary human interaction while keeping the complete technical envelope available for audit and diagnosis.
- Adopt Agent Operations Protocol v3 and Interaction Frame v2 with automatic non-authoritative delivery receipts and composite decision binding.
- Keep WorkBuddy and other Hosts limited to attachment transport, exact rendering, UI, MCP invocation, and explicit decision transport.
- Reject Host rewriting, omission, stale content, ambiguous continuation, stage reordering, and chat-memory recovery.
- Preserve Protocol v2 read, diagnosis, explicit safe cancellation/close, and explicit migration without fabricated historical views or receipts.
- Require candidate-package, real WorkBuddy DOCX, and independent Host cross-validation before acceptance closure.
- Add schema-validated professional Source-to-Harness analysis, Harness architecture assessment, explicit unsuitable/insufficient/no-change/reject explanations, and finite deterministic outcomes.
- Bind an immutable `EvolutionContextBinding` over Source, Catalog, Ontology and policy inputs, the non-secret Harness Advisor identity, intent, locale, and template version.
- Render every governed business stage through an Engine-owned presentation sandbox with fixed sections, order, labels, omission rules, locale, canonical bytes, and template digest.
- Enforce `AgentHostBoundaryContract`, `HostConformanceProfile`, and `CanonicalPresentationDeliveryReceipt` before a third-party Host can advance the lifecycle.
- Require at least one complete real WorkBuddy production lifecycle from a fresh Workspace, Session, and task. Preserve repeatability through deterministic multi-run and cross-Host conformance tests covering all four Engine-owned Harness business phases, Frame structure, business semantics, bound language, finite choices, and order.
- Exclude Host-owned chrome, loading, model-status, and transport-status surfaces from governed comparison while rejecting any Host rewrite, obstruction, replacement, acknowledgement, confirmation, inference, or advancement of Harness business content.

Proposal Review verdicts, Evaluation scoring, product ownership, human approval, publication authority, Source immutability, and the user-owned LLM configuration boundary remain unchanged.
