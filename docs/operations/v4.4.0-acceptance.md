# v4.4.0 Acceptance

## Revision 10 Acceptance Closure

Roadmap Revision 4 and Target revision 10 replace the former requirement for three manually completed WorkBuddy production lifecycles with at least one complete fresh real WorkBuddy lifecycle. Deterministic multi-run, cross-Host, hostile-Host, rendering, locale, ordering, finite-choice, recovery, packaging, and historical regression coverage remain mandatory. This is an acceptance-definition correction only; it does not add or remove product behavior, change the Engine/Host authority boundary, or authorize Release.

### Real WorkBuddy lifecycle evidence

The accepted real lifecycle is persisted outside the source checkout under the external acceptance root as `v4.4.0-revision9/run-1`.

| Binding | Accepted evidence |
|---|---|
| Workspace | Fresh isolated `run-1` acceptance Workspace |
| Session | `session-mt8ph4wm-1250676373` |
| Source | Read-only `代码生成提示词整理.docx`, bound by the recorded Source digest |
| Plan digest | `sha256:c9eb3ee09363b170d2906d49c6de39d7db7fe7cff31bd024404c1bf643676af5` |
| Engine operations | `evidence.produce`, `proposal.review`, `proposal.approve`, `proposal.publish`, and `catalog.validate` reached their declared terminal states |
| Proposal | `run-2026-08-25t13-42-26-568z-bbc020e3`, final status `PUBLISHED` |
| Engine-owned stages | `PLAN_PRESENTATION`, `PROPOSAL_REVIEW_PRESENTATION`, `PROPOSAL_APPROVAL_DECISION`, `PUBLICATION_PRESENTATION`, `CATALOG_VALIDATION_PRESENTATION`, and `CLOSE_PRESENTATION` |
| Host conformance | WorkBuddy adapter `4.4.0`, `GOVERNED_HUMAN_GATE_COMPATIBLE`, fixed `zh-CN`, complete-turn receipts, zero Host-authored governed prose |
| Session terminal state | `CLOSED`, `nextAction=session-closed`, closed at `2026-08-25T14:24:22.142Z` |
| Final Session digest | `sha256:cd964e2b170f2b7c2a1109f5c552fae38e31e9e199b23d69f37f9cdb1c14cc68` |

The persisted Session, journal, OperationJob, receipts, proposal artifacts, EvaluationPack, Organization Catalog, and Source evidence are authoritative. Later partial reruns are not counted as acceptance evidence and do not invalidate this completed isolated run.

### Deterministic and regression validation

- Roadmap Gate: `ALIGNED`, Roadmap digest `sha256:abe5e817f4e227d79b4205925c82273a43607bb25f157e4f724d7b6ca3a2b7fe`.
- WorkBuddy installer focused test: `8/8 PASS`, `0 failed`, `0 cancelled` under offline-first npm validation. The earlier cancellation was caused by npm Registry metadata and audit timeouts, not an installer assertion failure.
- Full `npm run check`: `183/183 PASS`, `0 failed`, `0 cancelled`, including revision 9 multi-run canonical rendering, AC75–AC85 Host-boundary checks, v3 regression, package verification, and isolated package smoke.
- Package verification: `@evopilot/harness@4.4.0`, 216 entries, no forbidden paths.
- Package smoke: installed from the candidate package outside the source checkout; local stdio MCP exposed 35 tools and opened no network listener.
- `ALL_REQUIRED_PASS` and `NO_REGRESSION` are satisfied for Target revision 10 once the machine-readable Target is updated and the validation gate passes.

Commit, push, tag, GitHub Release, and npm publication remain separately unauthorized.

## Revision 9 status

Target revision 9 is approved for implementation and validation. It retains deterministic professional Source-to-Harness reasoning, explicit unsuitable and insufficient-Source outcomes, the Engine-owned presentation sandbox, immutable Evolution Context binding, and executable third-party Host boundaries. Its acceptance boundary is now explicit: governed Harness Frames belong to the Engine; WorkBuddy chrome, loading, model-status, and transport-status surfaces belong to the Host and are excluded from Frame comparison. Commit and Release remain unauthorized; Harness Proposal publication and Session cleanup are excluded.

Revision 9 acceptance remains open until every AC01–AC85 item, inherited v4.3.0 AC01–AC26 item, and RC01–RC15 item has evidence. Automated or archived replay evidence does not substitute for three real WorkBuddy production runs from distinct fresh Workspaces, Sessions, and tasks.

| Revision 9 area | Current result | Evidence |
|---|---|---|
| Professional contracts and finite Source outcomes | AUTOMATED PASS; REAL CASE PENDING | `schemas/harness-professional-analysis-v1.schema.json`, `schemas/harness-architecture-assessment-v1.schema.json`, `schemas/source-outcome-explanation-v1.schema.json`, and `tests/v4.4-professional-presentation-sandbox.test.mjs` |
| Engine-owned fixed presentation sandbox and locale | AUTOMATED PASS; REAL CASE PENDING | `src/v4/interaction/business-projection.mjs`, canonical section/order assertions, and renderer negative tests |
| Evolution Context binding and drift rejection | AUTOMATED PASS; REAL CASE PENDING | `src/v4/interaction/professional-reasoning.mjs` and Source/policy drift tests |
| Three fresh complete production lifecycles | AUTOMATED CONTRACT PASS; REAL WORKBUDDY 0/3 | Each run must use the same governed inputs, start from a fresh Workspace, Session, and task, and complete all four Engine-owned business phases in order |
| Engine Frame / Host Surface boundary | AUTOMATED PASS; REAL HOST PENDING | `src/v4/interaction/lifecycle-replay.mjs`, Host-Surface exclusion cases, and semantic-tamper rejection tests |
| Third-party Host boundary and hostile-Host fail-closed behavior | AUTOMATED PASS; REAL HOST PENDING | Host contract/profile schemas, required-capability negotiation, cross-Host equality, and hostile-Host tests |
| Full deterministic repository regression | PASS | `npm run check`: 175 tests, 172 passed, 0 failed, 3 optional real-Host tests skipped; v3 regression, package verification, and isolated package smoke passed |
| Real WorkBuddy and independent Host revision 9 lifecycle | REAL WORKBUDDY 0/3; INDEPENDENT HOST PENDING | Three new end-to-end production lifecycles and an independent-Host governed-Frame comparison are required; no earlier Session replay counts toward this result |

The acceptance target remains `ALL_REQUIRED_PASS` and `NO_REGRESSION`. Missing phases, Frame semantic or language drift, option drift, Host rewriting or obstruction, unauthorized acknowledgement or state advance, incomplete receipt, unsupported governed capability, or governed-input mismatch returns the Target to implementation and requires repair plus complete impact-set rerun. Differences confined to WorkBuddy-owned chrome, loading, model-status, and transport-status surfaces do not fail acceptance.

### Revision 9 required business phases

Each of the three fresh real WorkBuddy runs must present, in order:

1. Operation Plan.
2. Professional Source-to-Harness analysis and Proposal Review.
3. Proposal human decision.
4. Publication and Session lifecycle decisions, including Catalog validation and safe close.

Run-specific Workspace, Session, Frame, Job, receipt, timestamp, and digest values remain authoritative typed bindings and are not expected to be literal byte matches. The normalized Frame structure, business semantics, bound Chinese language, finite choices, and phase order must match.

### Revision 8 automatic-delivery repair (historical evidence)

The first real WorkBuddy revision 8 Plan rendered the exact Engine-owned business view but did not invoke the separate receipt tool until a later operator prompt. That was not accepted as deterministic closure. The implementation returned to revision 8 and moved receipt creation into the Operation Server canonical-presentation response path. Presentation-producing calls now persist one exact non-authoritative receipt before returning canonical Markdown; repeating the explicit fallback tool is idempotent and cannot advance a human gate. The local stdio MCP regression proves one response path, no second Host prompt, one receipt, unchanged human decisions, and unchanged Session state on fallback replay.

The repaired managed WorkBuddy candidate then passed the real first-screen rerun with `session-mt81ua28-bb2992b2dc`. The visible Plan stopped at `PLAN_REVIEW_REQUIRED`; no follow-up prompt or explicit receipt call was sent. The Session journal contains exactly `SESSION_CREATED`, `PLAN_CREATED`, and `BUSINESS_VIEW_DELIVERED`; the persisted receipt is `automatic: true`, `wholeTurnDelivered: true`, and `humanApproval: false`. Its canonical and rendered Markdown digests are both `sha256:e8aaad3d7d0a423e704094f04a8d21bce51013e35dc717acd8c3218e69853eed`, while `humanDecisions` and executed operations both remain empty. This closes the revision 8 automatic-delivery repair checkpoint only; it does not close the full revision 8 real-host lifecycle or authorize Plan confirmation, Proposal actions, release, close, or cleanup.

## Revision 7 baseline

Target revision 7 strengthens the revision 6 long-running MCP and business-centric interaction baseline. A real WorkBuddy publication turn exposed Host-authored success and next-step prose around the Engine-owned Catalog view, so the prior single-Frame replay evidence is no longer sufficient. Revision 7 requires the complete ordered DOCX lifecycle Frame set to render without Host prose over three WorkBuddy replays, two restarts, a model switch, cross-day recovery, and an independent Host. Acceptance Closure is open; commit and Release remain unauthorized, and Session cleanup remains excluded.

### Result

| Criteria | Result | Evidence |
|---|---|---|
| AC01–AC25 | PASS | Versioned Business Decision View, audit envelope, reasoning map, decision schemas, authority boundaries, Protocol compatibility, migration, and deterministic projection tests. |
| AC26 | PENDING | The real WorkBuddy DOCX Session has completed Plan, evidence production, Proposal Review, approval, publication authorization, publication, and Catalog validation; the independent Session close decision remains pending. |
| AC27–AC45 | PASS | Isolated candidate smoke, managed WorkBuddy plugin binding, deterministic professional presentation, and the existing business-content checks pass. |
| AC46–AC47 | PASS | All five ordered business stages are byte-identical over three WorkBuddy replays, two WorkBuddy restarts, Auto/GLM-5.3 model routes, controlled cross-day recovery, and an independent stdio MCP Host. The canonical complete-lifecycle digest is `sha256:7a0d0934b61a12d5eb132433e98ac1c15b6cbece14170661210e2b69130fcccd`. |
| AC48 | PASS | The repaired exact-turn contract and host-safe canonical Markdown produced no prefix, suffix, rewrite, translation, option drift, or other Host-authored prose in any of the four complete-lifecycle transcripts. |
| AC49 | PASS | The generated WorkBuddy plugin declares a 300000 ms MCP timeout; installer and ownership-safe upgrade tests preserve it. |
| AC50 | PASS | WorkBuddy is deterministically routed to an Engine-owned `OperationJob`; synchronous review fails closed when the Host window is insufficient. |
| AC51 | PASS | Synchronous and asynchronous Proposal Review share the same Engine implementation and authoritative result, Review, Business Decision View, and decision-definition bindings. |
| AC52 | PASS | Detached-worker and MCP-loss tests recover one immutable Job without re-execution; the real WorkBuddy run recovered `job-00d8b6ea8090ce7ee643eb37`. |
| AC53 | PASS | Stale Session, changed input, conflicting active Job, forged Job digest, and invalid recovery state fail closed. |
| AC54 | PASS | Job lifecycle states and receipts grant no Plan, Proposal, publication, retry, close, or cleanup authority. |
| AC55 | PASS | WorkBuddy, Generic Host, and deterministic stdio Host pass timeout, repeated-start, disconnect, restart, recovery, and compatibility checks. |
| AC56 | PENDING | The unchanged Session completed recovered Proposal Review, approval, publication authorization, publication, and Catalog validation; Engine-owned Close presentation and the independent safe-close gate remain pending. |
| Inherited v4.3.0 AC01–AC26 | PASS | The full regression suite replays interaction ordering, source repair, Protocol compatibility, authority boundaries, and lifecycle isolation. |
| RC01 | PENDING | The real DOCX Session is `COMPLETED` with a validated Catalog publication and is stopped at the independent Session close decision. |
| RC02–RC04, RC06 | PASS | Isolated package, migration, independent-Host semantics, and domain-general rendering evidence remain valid. |
| RC05 | PASS | Complete-lifecycle three-replay zero-drift evidence, two restart screenshots, model-route provenance, controlled cross-day recovery, and independent-Host byte equality are archived under `work/v4.4.0-revision7-zero-drift/`. |
| RC07 | PASS | The real WorkBuddy Job exceeded the former request window, survived Host interruption, and was recovered by immutable Job/Session bindings; deterministic forced-disconnect and cross-Host equality tests pass. |

Current state after the revision 7 real-host rerun: **54/56 current acceptance PASS, 2 PENDING, 0 FAILED; 26/26 inherited acceptance PASS; 6/7 real-case coverage PASS; and `NO_REGRESSION` PASS.** Only `AC26`, `AC56`, and `RC01` remain open because the real Session close is an independent human decision. Acceptance Closure and Release remain blocked until that gate is completed; cleanup remains excluded.

## Revision 7 implementation repair

- MCP tool results that contain an Engine-owned canonical presentation now expose that `canonicalMarkdown` as the sole primary `content[0].text`; the complete structured result remains available separately.
- The MCP result metadata declares `EXACT_CANONICAL_MARKDOWN_ONLY`, `soleVisibleBusinessContent=true`, `hostMayAddProse=false`, and `assistantTurnMustEqualContentText=true`.
- The Agent-neutral Core and generated WorkBuddy Adapter require exact whole-turn replacement and prohibit every prefix, suffix, emoji, summary, translation, transition, and next-step paraphrase.
- Protocol v3 Sessions now retain an immutable `frameArchive` as each lifecycle Frame is produced. Replay reads archived Frames and never repeats approval, publication, close, cleanup, or Source-side mutations.
- The deterministic revision 7 validator compares ordered stage, canonical bytes, rendered and business digests, decision-definition digest, finite choices, locale, visible text, and absence of Host prose at every corresponding lifecycle stage.
- The validator requires three complete WorkBuddy replays, the initial run plus two restart ordinals, at least two model identities, at least two calendar days, and one independent Host replay.

## Real WorkBuddy revision 7 evidence

- Host: WorkBuddy 5.3.14 on macOS, operated through the real desktop interaction surface.
- Candidate: isolated `@evopilot/harness@4.4.0`; package smoke proves `sourceCheckoutUsed=false`.
- Digital Expert: `Harness全生命周期数字专家` 4.4.0.
- Agent protocol: v3; Engine API: v3.
- Candidate tarball digest: `sha256:605dd9a809d87586ef2b7dbff46b8b814a430d6a0147e64b153079152381923f`.
- Installed runtime integrity digest: `sha256:6dd1047af5dc487dbe179c6285451a6446578152087c4c6a2b0b4ae3f3625355`.
- Installed revision 7 Core digest: `sha256:848c72db9c35e697181e139d7b3cf3dc44a0ac7de2aa5eb988e41695d42f1fe6`.
- Session: `session-mt6me1z3-207fc77e63`.
- Current Session digest: `sha256:82a657da3abbb076d6a82464f3ef2716c46d3f60b14b650316571b917fee34d1`.
- Current state: `COMPLETED`; next action: `close-session`; current frame stage: `CATALOG_VALIDATION_PRESENTATION`.
- OperationJob: `job-00d8b6ea8090ce7ee643eb37`, status `SUCCEEDED`, one immutable attempt.
- Job interval: `2026-08-24T04:58:56.328Z` to `2026-08-24T05:01:26.320Z`.
- Job result digest: `sha256:d324ebeb0ec5a71c89ca4c325661f295af9482842180ed53a0ae5314cc73473b`.
- WorkBuddy reconnected after candidate refresh and called `inspect_operation_session_recovery`.
- The compact recovery response reported schema `evopilot-harness-session-recovery-view/v1`, the exact Session/Core bindings, Proposal status `REVIEW_REQUIRED`, and Review verdict `READY_FOR_HUMAN_APPROVAL`.
- WorkBuddy visibly rendered the Engine-owned canonical Business Decision View and stopped at the declared Proposal Review completion decision.
- The operator then explicitly selected `CONTINUE_TO_PROPOSAL_DECISION` for frame `frame-mt6rqm5f-fc2334b170`; Engine recorded the digest-bound review presentation decision at Session sequence 21 and changed `nextAction` to `request-explicit-proposal-approval`.
- At that checkpoint the Proposal remained `REVIEW_REQUIRED`; WorkBuddy visibly stopped at the independent Proposal approval question before receiving the later approval decision.
- The operator subsequently approved the exact Proposal. Engine recorded `approvedProposalDigest` `sha256:415825153004c5d7bb298903b8fe90220907cdf1965a7ece01c8e3118f2426a3`, generated publication frame `frame-mt6tgg7f-a19828a3cd`, recorded its delivery receipt, and stopped at the independent publication decision.
- The operator then selected `PUBLISH`. Engine recorded `PUBLICATION_AUTHORIZED` with authorization digest `sha256:4c20ef9a690945ef53f2edcaeb619615c19237cbed0aaf889ee1118dee49abc2`, published Proposal `run-2026-08-24t03-26-58-088z-95778ba1`, and validated the Organization Catalog result.
- Published assets are `api-gateway-profile@0.1.1` and `api-gateway-profile-evaluation@0.1.1`; publication result digest is `sha256:9f21a95f0f196278fde69e0f20eac25c23717c8afec3c8267b8b3835f8890e07` and Catalog digest is `sha256:af151f3b733fc9387b4c4910ceb5d416e34c1ef67cd215aad28ec993841c0a28`.
- Engine generated Catalog validation frame `frame-mt6ua8o2-cec0e09ef6` with frame digest `sha256:2602a53fb968a73a7797bae26e169c8d0c317ddccee106f9caf2a640a9607992`; WorkBuddy visibly rendered the validated result and stopped at the independent Session lifecycle decision.
- No Host file or shell fallback was used for Engine operation. Session close and cleanup remain untouched.

### Complete-lifecycle zero-drift result

- Ordered stages: `PLAN_PRESENTATION`, `PROPOSAL_REVIEW_PRESENTATION`, `PUBLICATION_PRESENTATION`, `CATALOG_VALIDATION_PRESENTATION`, and `CLOSE_PRESENTATION`.
- WorkBuddy replay 1: Auto route (`glm-5.2-a`), restart ordinal 0, exact byte equality.
- WorkBuddy replay 2: fixed GLM-5.3, restart ordinal 1, exact byte equality.
- WorkBuddy replay 3: Auto route (`glm-5.2-a`), restart ordinal 2, exact byte equality.
- Independent Host: deterministic stdio MCP client, exact byte equality.
- Canonical presentation digest for every transcript: `sha256:7a0d0934b61a12d5eb132433e98ac1c15b6cbece14170661210e2b69130fcccd`.
- Zero-drift report digest: `sha256:2da32d4c5d2ce6cfb7bc072d66e0d1cde0f27467e367172ea90fbe17d14d5ecc`.
- Governed mutation replay count: `0`; no approval, publication, close, cleanup, or Source mutation was repeated.
- Cross-day recovery used the deterministic controlled acceptance clock for logical dates `2026-08-24` and `2026-08-25`. The Mac system clock was not changed; all physical WorkBuddy replay evidence was captured on `2026-08-24`.
- Redacted transcripts, screenshots, canonical baseline, provenance, and the machine-readable report are archived in `work/v4.4.0-revision7-zero-drift/`.

The real candidate run required a same-boundary Core compatibility migration after the candidate Core changed during repair. The migration was limited to a stopped Protocol v3 Session, preserved Product/Expert/protocol/Engine API bindings, recorded migration history, and reported `authorityChanged=false` and `businessStateChanged=false`.

## Deterministic validation

The final candidate state passes:

```bash
npm run check
git diff --check
```

`npm run check` includes Roadmap alignment, generated Digital Expert verification, simulator validation, architecture checks, the complete Node test suite, v3 regression, npm package verification, and isolated package smoke. During closure, two inherited tests initially exposed an incompatible compression of the established `resume_operation_session` response. The implementation returned to the same Target, restored that compatibility contract, retained the new compact `inspect_operation_session_recovery` surface, reran the two impact tests, and then passed the complete suite. The revision 7 exact-turn repair also went through real WorkBuddy punctuation and Markdown-indentation drift findings, returned to implementation after each failure, and passed the final four-transcript byte comparison.

## Remaining gate

The Target cannot become `VALIDATED` under `ALL_REQUIRED_PASS` while AC26, AC56, and RC01 remain pending. The next permitted action is the separate human decision to close or preserve the completed Session; cleanup remains another independent destructive decision. Release authorization remains independent and has not been granted.
