# v4.2.3 Candidate Acceptance

Target revision 2 implements the Harness-owned LLM initialization closure. The implementation is complete and all 18 acceptance criteria pass. AC18 is satisfied by a real WorkBuddy run against the v4.2.3 npm candidate tarball with no source-checkout resolution. Commit, push, tag, GitHub Release, and npm publication remain explicitly unauthorized.

## Passing evidence

- Roadmap Gate and implementation Evolution Gate: PASS.
- Digital Expert generation and Core digest validation: PASS.
- Architecture boundaries: PASS.
- Full repository suite: 135 tests passed.
- `package:verify`: PASS with 180 runtime entries and no forbidden paths.
- Clean installed-tarball smoke: PASS with 21 MCP tools and no source checkout.
- Real WorkBuddy `2.106.4` candidate-package end to end: PASS for v4.2.3, exact Core digest, stdio MCP, LLM initialization, live doctor, `CONFIGURED_AND_VERIFIED`, default Workspace reuse, `0600` verification receipt, preserved `models.json`, and a credential-free transcript.
- LLM initialization tests: installation/readiness separation, absent configuration, no overwrite, `0600` receipt, live doctor, secret redaction, drift invalidation, Workspace reuse, and Release-boundary rejection all pass.

## Post-release mandatory verification

Public Registry verification is deliberately separate from candidate acceptance. It is mandatory after a future separately authorized publication and does not authorize or perform that publication.

After a separately authorized publication, rerun:

```bash
npm view @evopilot/harness@4.2.3 version
npm run package:workbuddy -- --package-spec @evopilot/harness@4.2.3 --evidence-dir <external-evidence-directory>
```

The public-package run must repeat the same live initialization and default-Workspace reuse scenario and retain a redacted report as post-release evidence. A candidate report must never be relabeled as public Registry evidence.
