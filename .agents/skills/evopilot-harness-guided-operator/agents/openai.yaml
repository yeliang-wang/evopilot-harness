interface:
  display_name: "EvoPilot Harness Guided Operator (Compatibility)"
  short_description: "Routes legacy invocations to the v4 Digital Expert"
  default_prompt: "Load the generated evopilot-harness-digital-expert Skill and operate the installed Release through its local stdio MCP Operation Server. Do not use the former human-CLI workflow."

policy:
  allow_implicit_invocation: false
