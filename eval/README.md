# EvoPilot Harness Evaluation Fixtures

This directory contains regression fixtures for `evopilot-harness` itself.

The fixtures are not production matching rules. They are hidden-oracle test cases used by `evopilot-harness eval run` and `evopilot-harness llm replay` to verify that unknown source inputs produce reviewable, explainable, and stable Harness decisions.
