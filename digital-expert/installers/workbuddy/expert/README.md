# EvoPilot Harness Digital Expert for WorkBuddy

This package is installed and registered by `evopilot-harness agent install --host workbuddy`. Its Agent uses the packaged Digital Expert instructions and the exact-version local stdio MCP server. Review, approval, and publication remain separate human decisions.
